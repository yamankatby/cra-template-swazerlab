import React from 'react';

const NotFound = () => (
	<div className='crp'>
		<h2>404 Not Found</h2>
		<p>
			Either you're requesting an invalid URI or the resource in question doesn't exist<br /> (ex: no such user).
		</p>
	</div>
);

export default NotFound;
